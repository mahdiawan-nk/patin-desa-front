
services:
  frontend:
    build: .
    container_name: scm-frontend
    working_dir: /app
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NITRO_PORT=3000
      - NITRO_HOST=0.0.0.0
    command: npm run dev -- --host 0.0.0.0
